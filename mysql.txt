### mysql usage notes ###

# modify mysql root password #
	$ mysql -u root
	mysql> update mysql.user set password = password('mysql') where user = 'root';
	mysql> flush privileges;

# login mysql with root and password #
	$ mysql -u root -p
	$ mysql -h <host> -p -u root


# check exists database #
	mysql> show databases;
	mysql> use information_schema;
	mysql> show tables;
	mysql> show tables from information_schema;
	mysql> show columns from schemata;
	mysql> help show;

# manipulate database #
	mysql> create database technology;
	mysql> drop database technology;

# manipulate table #

mysql> create table video_tbl(
    -> video_id INT NOT NULL AUTO_INCREMENT,
    -> video_name VARCHAR(100) NOT NULL,
    -> video_author VARCHAR(50) NOT NULL,
    -> comp_date DATE,
    -> primary key (video_id)
    -> );

mysql> drop table video_tbl;


mysql> insert into video_tbl
    -> (video_name, video_author, comp_date)
    -> values
    -> ("Python", "James", NOW());

mysql> select * from video_tbl;

mysql> select * from video_tbl where video_author="James";

mysql> update video_tbl
    -> set video_author='Zoe'
    -> where video_id=3;

mysql> delete from video_tbl where video_id=6;

mysql> select * from video_tbl where video_author like '%es';

mysql> select * from video_tbl order by video_author asc;
mysql> select * from video_tbl order by video_author desc;


mysql> select a.video_id, a.video_author, b.movie_count
    -> from video_tbl a, movie_cnt b
    -> where a.video_author = b.video_author;

mysql> select * from movie_cnt where movie_count is NULL;
mysql> select * from movie_cnt where movie_count is NOT NULL;


	## mysql create new account 'user/pass' and grant access authority ##
	## with mysql root login ##
	> grant all on <database>.* to <new_user>@<host> identified by <password>

	## check databases and tables;
	> show databases;
	> use <database>;
	> show tables;

	## mysql create/drop database
	> create database sql_basic;
	> drop database sql_basic;

	## mysql create/delete table
	> create table <table_name> ( field1 args, field2 args, ..);

	## mysql rename table name
	> rename table <old_name> to <new_name>

	## table constraint: primary key

	## drop/alter table
	> drop table <table_name>;
	> alter table <table_name> add column (fields define);
	> alter table <table_name> del column (fields define);
